# LiveKit (فاز ۶) — اختیاری، کنار HLS/RTMP موجود

LiveKit **جایگزین** RTMP/HLS فعلی نیست — کاملاً کنارش اجرا می‌شه. اگه فعالش
نکنی، هیچی توی رفتار فعلی سایت عوض نمی‌شه.

## فعال‌سازی موقع نصب
```bash
sudo bash install.sh <IP-سرور> --with-livekit
```
این کار می‌کنه: `livekit-server` + `redis` + `livekit-ingress` رو با Docker
بالا میاره (پوشه‌ی `/opt/livekit`)، کلیدها رو می‌سازه، و به `.env` بک‌اند اضافه
می‌کنه.

## چرا هنوز "کامل و بدون باگ" یعنی چی، دقیق‌تر
یه نسخه‌ی اول از این (از یه چت‌بات دیگه) رو چک کردم و باگ داشت: از یه کلاس
(`LiveKitAPI`) استفاده کرده بود که اصلاً توی SDK وجود نداره (`ingress.createIngress`
مستقیم روی `IngressClient` هست، نه یه wrapper). این‌جا با `IngressClient` واقعی
جایگزین شده و مستندات رسمی چک شده.

با این حال، صادقانه بگم چیزهایی هست که **قابل تأیید صددرصد نبود** بدون یه
سرور واقعی برای تست:
- فرمت دقیق `rtmp_base_url` (فرض کردم `rtmp://IP:1936/live` — این پیشوندیه
  که خود LiveKit بهش کلید استریم اضافه می‌کنه؛ مقدار نهایی همیشه از
  پاسخ واقعی API (`ensureIngress`) خونده می‌شه، نه از این فرض دستی — پس
  حتی اگه فرمت پیشوند کمی فرق داشته باشه، چیزی که توی پنل ادمین نشون داده
  می‌شه همون چیزیه که سرور واقعی برگردونده.
- رفتار دقیق enum مقادیر `ConnectionState` توی `livekit-client` سمت
  فرانت‌اند (کد فرض کرده رشته‌های ساده مثل `'connected'`).

## نکته‌ی مهم عملیاتی: OBS باید به دو مقصد پخش کنه
LiveKit Ingress یه سرور RTMP کاملاً جدا و مجزاست (پورت `1936`، **نه** همون
`1935` که مدیا سرور خودمون ازش استفاده می‌کنه — این عمداً جدا نگه داشته شده
تا با پخش فعلی OBS تداخل نکنه). یعنی اگه می‌خوای هم HLS قدیمی هم LiveKit
همزمان کار کنن، معلم باید توی OBS از قابلیت «چند خروجی» (یا یه پلاگین مثل
Restream) استفاده کنه و همزمان به هر دو آدرس RTMP پخش کنه. این محدودیت
واقعیه، نه چیزی که بشه دورش زد بدون یه سرویس relay اضافه.

## پورت‌های باز شده (فقط با `--with-livekit`)
- `7880/tcp` — سیگنالینگ WebSocket (بدون TLS، چون کل پروژه فعلاً HTTPS نداره)
- `7881/tcp` — ICE روی TCP (fallback)
- `50000-50100/udp` — رسانه‌ی WebRTC
- `1936/tcp` — RTMP مخصوص LiveKit Ingress (جدا از `1935` قبلی)
- `3478/udp` — TURN relay داخلی LiveKit برای شبکه‌هایی که اتصال مستقیم WebRTC در آن‌ها ممکن نیست

## اولویت پخش و TURN

در تنظیمات پنل owner سه حالت وجود دارد:

- **خودکار:** اول LiveKit، در صورت شکست اتصال HLS
- **فقط LiveKit:** برای اطمینان از اینکه تأخیر HLS هرگز به‌عنوان مسیر جایگزین استفاده نشود
- **فقط HLS:** حالت عیب‌یابی یا سازگاری

LiveKit قبل از fallback به HLS، اتصال را از مسیرهای مستقیم WebRTC و TURN امتحان
می‌کند. نصب‌کننده TURN داخلی LiveKit را روی UDP پورت `3478` فعال می‌کند؛ برای
کارکرد آن، این پورت علاوه بر بازه‌ی رسانه‌ی WebRTC باید در firewall و فایروال
ابری سرور هم باز باشد.

## استفاده‌ی فرانت‌اند
صفحه‌ی `channel/[username]` خودش چک می‌کنه (`GET /api/livekit/status`) —
اگه فعال بود از `LiveKitPlayer` استفاده می‌کنه، وگرنه دقیقاً همون
`HlsPlayer` قبلی. یعنی هیچ خطری برای رفتار فعلی نداره.

## چیزی که هنوز نساختم
- UI برای غیرفعال‌کردن/حذف Ingress (فقط ساختش توی پنل ادمینه، دکمه‌ی
  حذف نداره — endpoint بک‌اندش (`DELETE /api/livekit/channels/:channel/ingress`)
  آماده‌ست)
- Egress (ضبط/خروجی به یوتیوب و امثالش) — اصلاً جزو این فاز نبود
- تست واقعی روی یه سرور — همه‌چی اینجا سینتکسش چک شده، ولی هیچ‌وقت
  واقعاً روی یه VPS با UDP باز اجرا نشده
